---
phase: quick
plan: 260407-l44
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - install.sh
  - install.ps1
autonomous: true
requirements: []
must_haves:
  truths:
    - "README reflects Phase 4 completion with unified parser, DigitalOcean PDF, Playwright fix"
    - "Install scripts install markdown-it-py instead of markdown, and download md_renderer.py"
    - "All 13 local commits are pushed to GitHub origin/master"
  artifacts:
    - path: "README.md"
      provides: "Updated project documentation"
    - path: "install.sh"
      provides: "Fixed bash installer with markdown-it-py and md_renderer.py"
    - path: "install.ps1"
      provides: "Fixed PowerShell installer with markdown-it-py and md_renderer.py"
  key_links: []
---

<objective>
Update README.md for v1.0 final, fix install scripts for Phase 4 dependencies, and push all local commits to GitHub.

Purpose: The repo has 13 unpushed commits from Phase 4 (generation quality overhaul) and earlier quick fixes. The README needs to reflect the current state, install scripts need to include markdown-it-py (replaced markdown) and md_renderer.py (new file), and everything needs to be pushed.

Output: Updated README.md, fixed install.sh/install.ps1, all commits on GitHub.
</objective>

<execution_context>
@.planning/quick/260407-l44-push-all-local-commits-to-github-and-upd/260407-l44-PLAN.md
</execution_context>

<context>
@README.md
@install.sh
@install.ps1
@skills/pbi-docgen/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md and fix install scripts for Phase 4</name>
  <files>README.md, install.sh, install.ps1</files>
  <action>
**README.md** -- Rewrite to reflect v1.0 final state. Keep the same structure but update content:

1. **Header** -- Keep "DocsGen -- PBI Documentation Skill" title and tagline
2. **What it does** -- Add bullet points for Phase 4 improvements:
   - Unified Markdown parser (markdown-it-py) for consistent formatting
   - DigitalOcean-inspired code block styling (left-accent pattern)
   - Professional PDF output via Playwright/Chromium
   - English or French output per run
   - Audience-appropriate depth (executive, IT, client)
3. **What's New in v1.0** -- Add a new section after "What it does":
   - Unified parser: markdown-it-py replaces hand-rolled TABLE:/CODE_BLOCK: markers for reliable Markdown rendering in both DOCX and PDF
   - DigitalOcean-style code blocks: left-accent border pattern in both Word and PDF output
   - Playwright fix: uses `wait_until="domcontentloaded"` to prevent hangs on PDF generation
   - Standard Markdown prompts: all 12 section templates now emit standard Markdown (headings, tables, fenced code) instead of custom markers
   - Explicit heading sizes: h1=20pt, h2=14pt, h3=12pt, h4=11pt
4. **Install** -- Keep the install section as-is (curl/irm one-liners unchanged)
5. **Usage** -- Keep as-is
6. **Requirements** -- Update to mention markdown-it-py:
   - Claude Code CLI
   - Python 3.9+
   - The install script handles all Python deps automatically
7. **Manual install** -- Update pip install line: replace `markdown` with `markdown-it-py` in the pip install command:
   ```
   pip install python-docx==1.2.0 Jinja2==3.1.6 playwright==1.58.0 markdown-it-py PyYAML Pillow lxml
   ```
8. **Asset folder structure** -- Keep as-is
9. **Stack** -- Add markdown-it-py row:
   | Markdown parsing | markdown-it-py 4.0.0 |
10. **Related** -- Keep as-is

**install.sh** -- Two fixes:
1. Line 68: Change `markdown` to `markdown-it-py` in the pip install command
2. After the `download "scripts/utils.py"` line (line 43), add: `download "scripts/md_renderer.py"`

**install.ps1** -- Two fixes:
1. Line 65: Change `markdown` to `markdown-it-py` in the pip install command
2. After the `Download-File "scripts\utils.py"` line (line 40), add: `Download-File "scripts\md_renderer.py"`
  </action>
  <verify>
    <automated>grep -q "markdown-it-py" README.md && grep -q "markdown-it-py" install.sh && grep -q "markdown-it-py" install.ps1 && grep -q "md_renderer" install.sh && grep -q "md_renderer" install.ps1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md reflects v1.0 final with Phase 4 features, install scripts install markdown-it-py (not markdown) and download md_renderer.py</done>
</task>

<task type="auto">
  <name>Task 2: Commit updates and push all commits to GitHub</name>
  <files>README.md, install.sh, install.ps1</files>
  <action>
1. Stage the three modified files: `git add README.md install.sh install.ps1`
2. Commit with message: "docs: update README for v1.0 final and fix install scripts for Phase 4 deps"
3. Push all commits to GitHub: `git push origin master`
4. Verify push succeeded by checking `git log --oneline origin/master -3`
  </action>
  <verify>
    <automated>git log --oneline origin/master -1 | grep -q "README" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All local commits (including the new README/install fix commit) are pushed to origin/master on GitHub</done>
</task>

</tasks>

<verification>
- README.md contains "markdown-it-py", "DigitalOcean", "v1.0" content
- install.sh and install.ps1 both reference markdown-it-py and md_renderer.py
- `git status` shows clean working tree (no unpushed commits)
- `git log --oneline origin/master..master` returns empty (all pushed)
</verification>

<success_criteria>
- README accurately describes v1.0 final state with Phase 4 features
- Install scripts install the correct dependencies (markdown-it-py, not markdown)
- Install scripts download md_renderer.py (new Phase 4 file)
- All commits are on GitHub
</success_criteria>

<output>
After completion, create `.planning/quick/260407-l44-push-all-local-commits-to-github-and-upd/260407-l44-SUMMARY.md`
</output>
