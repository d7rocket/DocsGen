---
phase: 01-skill-core-intake-docx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/pbi-docgen/scripts/md_parser.py
  - ~/.claude/skills/pbi-docgen/scripts/utils.py
  - ~/.claude/skills/pbi-docgen/references/section_heading_map.yaml
  - ~/.claude/skills/pbi-docgen/references/fowler_guidance.md
autonomous: true
requirements: [SCAF-01, SCAF-02, SCAF-03, PARSE-01, PARSE-02, PARSE-03]

must_haves:
  truths:
    - "Skill directory structure exists at ~/.claude/skills/pbi-docgen/ with scripts/, templates/prompts/, references/ subdirectories"
    - "MD parser can read a Markdown file and return a dict of detected sections keyed by section ID"
    - "Sections inside fenced code blocks are not falsely detected as headings"
    - "Sections with no matched content are omitted from parser output"
    - "Multiple source MD files are concatenated before parsing"
  artifacts:
    - path: "~/.claude/skills/pbi-docgen/scripts/md_parser.py"
      provides: "Markdown section detection and extraction"
      exports: ["parse_markdown_sources"]
    - path: "~/.claude/skills/pbi-docgen/scripts/utils.py"
      provides: "Shared helpers: color parsing, file validation, path resolution"
      exports: ["parse_hex_color", "validate_file_exists", "resolve_absolute_path", "ensure_directory"]
    - path: "~/.claude/skills/pbi-docgen/references/section_heading_map.yaml"
      provides: "Heading keyword to section category mapping"
      contains: "sections:"
    - path: "~/.claude/skills/pbi-docgen/references/fowler_guidance.md"
      provides: "Fowler's Modern English Usage rules for prompt engineering"
      contains: "active voice"
  key_links:
    - from: "~/.claude/skills/pbi-docgen/scripts/md_parser.py"
      to: "~/.claude/skills/pbi-docgen/references/section_heading_map.yaml"
      via: "YAML load for keyword matching"
      pattern: "yaml\\.safe_load|section_heading_map"
---

<objective>
Create the skill directory scaffolding, reference files, shared utilities, and Markdown parser that form the foundation for all subsequent plans.

Purpose: Establishes the file structure per Claude Code skill conventions (SCAF-03), provides the MD parsing pipeline that all content generation depends on, and creates shared utilities used by every downstream module.

Output: Skill directory at ~/.claude/skills/pbi-docgen/ with scripts/md_parser.py, scripts/utils.py, references/section_heading_map.yaml, references/fowler_guidance.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skill-core-intake-docx/01-CONTEXT.md
@.planning/phases/01-skill-core-intake-docx/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skill directory structure and reference files</name>
  <files>
    ~/.claude/skills/pbi-docgen/references/section_heading_map.yaml
    ~/.claude/skills/pbi-docgen/references/fowler_guidance.md
  </files>
  <read_first>
    .planning/phases/01-skill-core-intake-docx/01-RESEARCH.md (Section Heading Map example and Fowler Rules template)
    .planning/phases/01-skill-core-intake-docx/01-CONTEXT.md (D-10, D-11, D-23, D-24 decisions)
  </read_first>
  <action>
Create the full skill directory tree per D-23:

```
mkdir -p ~/.claude/skills/pbi-docgen/scripts
mkdir -p ~/.claude/skills/pbi-docgen/templates/prompts
mkdir -p ~/.claude/skills/pbi-docgen/references
```

Create `references/section_heading_map.yaml` with this exact content (per D-11):

```yaml
# Maps ## heading keywords to section categories (D-11)
# Keywords are matched case-insensitively against ## headings in source MD
sections:
  - id: overview
    label: "Project Overview"
    keywords: ["overview", "summary", "introduction", "about"]
  - id: sources
    label: "Source Systems & Architecture"
    keywords: ["data source", "gateway", "connection", "source system", "architecture"]
  - id: dataflows
    label: "Dataflows"
    keywords: ["dataflow", "data flow", "refresh", "pipeline"]
  - id: mquery
    label: "M Query Business Logic"
    keywords: ["m query", "power query", "m code", "query", "transformation"]
  - id: datamodel
    label: "Data Model"
    keywords: ["data model", "ssas", "relationship", "table", "measure", "dax", "column"]
  - id: maintenance
    label: "Troubleshooting & Maintenance"
    keywords: ["parameter", "troubleshooting", "maintenance", "configuration", "setting"]
```

Create `references/fowler_guidance.md` with Fowler's Modern English Usage rules (per D-24):

```markdown
# Fowler's Modern English Usage - Prompt Rules

These rules are embedded in every section generation prompt to enforce clear, direct English prose.

## Rules

1. **Prefer active voice.** "The report filters data" not "Data is filtered by the report."
2. **No nominalization padding.** "Using" not "the utilization of." "Analyzing" not "the analysis of."
3. **No corporate filler.** Never use: leverage, synergy, optimize, utilize, facilitate, streamline, robust, scalable, holistic, best-in-class, cutting-edge, world-class.
4. **Keep sentences under 25 words.** Split long sentences into two.
5. **Be direct.** State what something does, not what it "aims to" or "seeks to" do.
6. **Prefer concrete nouns over abstract ones.** "The table stores customer records" not "The data storage mechanism maintains entity information."
7. **Use plain connectives.** "because" not "due to the fact that." "so" not "as a consequence of."

## Application

Include these rules verbatim (or the Jinja2 include of fowler_rules.j2) at the top of every section generation prompt. The LLM must treat them as hard constraints, not suggestions.
```
  </action>
  <verify>
    <automated>ls -la ~/.claude/skills/pbi-docgen/scripts/ ~/.claude/skills/pbi-docgen/templates/prompts/ ~/.claude/skills/pbi-docgen/references/ && cat ~/.claude/skills/pbi-docgen/references/section_heading_map.yaml | head -5 && cat ~/.claude/skills/pbi-docgen/references/fowler_guidance.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Directory ~/.claude/skills/pbi-docgen/scripts/ exists
    - Directory ~/.claude/skills/pbi-docgen/templates/prompts/ exists
    - Directory ~/.claude/skills/pbi-docgen/references/ exists
    - section_heading_map.yaml contains "sections:" on first non-comment line
    - section_heading_map.yaml contains all 6 section IDs: "overview", "sources", "dataflows", "mquery", "datamodel", "maintenance"
    - fowler_guidance.md contains "active voice"
    - fowler_guidance.md contains "nominalization"
    - fowler_guidance.md contains "corporate filler"
  </acceptance_criteria>
  <done>Skill directory tree exists with all subdirectories; section_heading_map.yaml defines 6 sections with keyword arrays; fowler_guidance.md contains 7 Fowler rules</done>
</task>

<task type="auto">
  <name>Task 2: Create shared utilities module</name>
  <files>~/.claude/skills/pbi-docgen/scripts/utils.py</files>
  <read_first>
    .planning/phases/01-skill-core-intake-docx/01-CONTEXT.md (D-09 docsgen-assets creation, D-22 JSON config schema)
    .planning/phases/01-skill-core-intake-docx/01-RESEARCH.md (Pitfall 6: image path resolution)
  </read_first>
  <action>
Create `scripts/utils.py` with these exact functions:

1. `parse_hex_color(hex_str: str) -> tuple[int, int, int]` - Takes a hex color string with or without '#' prefix (e.g., "#1B365D" or "1B365D"), returns (r, g, b) tuple of ints. Raises ValueError if format is invalid. Strips whitespace.

2. `validate_file_exists(path: str, label: str) -> str` - Takes a file path and a human-readable label (e.g., "client logo"). Returns the absolute resolved path if file exists. Raises FileNotFoundError with message: f"{label} not found: {path}".

3. `resolve_absolute_path(path: str, base_dir: str) -> str` - If path is already absolute, returns it. Otherwise joins with base_dir and resolves. Uses os.path.abspath.

4. `ensure_directory(path: str) -> str` - Creates directory and all parents if not existing (os.makedirs with exist_ok=True). Returns the path. Used for docsgen-assets/ and docsgen-output/ creation per D-09 and INTAK-04.

5. `setup_asset_directories(working_dir: str) -> dict[str, str]` - Creates `docsgen-assets/`, `docsgen-assets/logos/`, `docsgen-assets/source/` under working_dir per D-09. Returns dict with keys "assets", "logos", "source" mapping to absolute paths.

6. `load_json_config(config_path: str) -> dict` - Reads and parses a JSON config file. Validates required keys exist: client_name, client_logo, company_logo, primary_color, accent_color, language, audience, report_name, version, source_files, output_dir (per D-22). Raises ValueError listing any missing keys.

Module-level: `import os, json, sys`

Include type hints on all functions. Include docstrings with one-line descriptions.
  </action>
  <verify>
    <automated>cd ~/.claude/skills/pbi-docgen && python -c "from scripts.utils import parse_hex_color, validate_file_exists, resolve_absolute_path, ensure_directory, setup_asset_directories, load_json_config; print('imports OK'); assert parse_hex_color('#1B365D') == (27, 54, 93); assert parse_hex_color('FF0000') == (255, 0, 0); print('parse_hex_color OK')"</automated>
  </verify>
  <acceptance_criteria>
    - utils.py contains "def parse_hex_color("
    - utils.py contains "def validate_file_exists("
    - utils.py contains "def resolve_absolute_path("
    - utils.py contains "def ensure_directory("
    - utils.py contains "def setup_asset_directories("
    - utils.py contains "def load_json_config("
    - parse_hex_color('#1B365D') returns (27, 54, 93)
    - parse_hex_color('FF0000') returns (255, 0, 0)
    - All functions have type hints and docstrings
    - load_json_config validates these required keys: client_name, client_logo, company_logo, primary_color, accent_color, language, audience, report_name, version, source_files, output_dir
  </acceptance_criteria>
  <done>utils.py exports 6 functions with type hints and docstrings; parse_hex_color correctly parses hex colors; load_json_config validates the D-22 config schema</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Markdown parser with section detection</name>
  <files>
    ~/.claude/skills/pbi-docgen/scripts/md_parser.py
    ~/.claude/skills/pbi-docgen/scripts/test_md_parser.py
  </files>
  <read_first>
    ~/.claude/skills/pbi-docgen/references/section_heading_map.yaml (keyword definitions created in Task 1)
    ~/.claude/skills/pbi-docgen/scripts/utils.py (resolve_absolute_path helper created in Task 2)
    .planning/phases/01-skill-core-intake-docx/01-CONTEXT.md (D-10, D-11, D-12, D-13 decisions)
    .planning/phases/01-skill-core-intake-docx/01-RESEARCH.md (Pitfall 5: code block false positives)
  </read_first>
  <behavior>
    - Test 1: Single file with 3 ## headings matching overview, mquery, datamodel returns dict with exactly those 3 keys
    - Test 2: Heading inside a fenced code block (``` ... ## Fake Heading ... ```) is NOT detected as a section
    - Test 3: File with no matching headings returns empty dict
    - Test 4: Heading "## Data Source Configuration" matches section ID "sources" (keyword "data source")
    - Test 5: Multiple files are concatenated — sections from both files appear in output
    - Test 6: Section with heading but no body content below it is excluded (PARSE-03 / D-12)
    - Test 7: Keywords are matched case-insensitively ("## PROJECT OVERVIEW" matches "overview")
  </behavior>
  <action>
Create `scripts/md_parser.py` with:

1. `_load_section_map(map_path: str) -> list[dict]` - Internal function. Loads section_heading_map.yaml via PyYAML. Returns the list under the "sections" key. The map_path defaults to `os.path.join(os.path.dirname(__file__), '..', 'references', 'section_heading_map.yaml')`.

2. `_is_in_code_block(lines: list[str], line_index: int) -> bool` - Internal function. Counts opening/closing ``` fence markers from line 0 to line_index-1. If count is odd, the line is inside a code block. Per Pitfall 5 in RESEARCH.md.

3. `_match_heading_to_section(heading_text: str, section_map: list[dict]) -> str | None` - Internal function. Lowercases heading_text, checks if any keyword from any section entry is a substring of the heading. Returns the section id (e.g., "overview") or None. First match wins (sections are ordered by priority in the YAML).

4. `parse_markdown_sources(source_files: list[str]) -> dict[str, dict]` - Main public function.
   - Reads and concatenates all source files (per D-13), separated by a blank line
   - Scans for lines matching `^##\s+(.+)` regex (per D-10)
   - Skips lines inside fenced code blocks (Pitfall 5)
   - For each matched heading, calls _match_heading_to_section
   - Collects all text between matched headings as that section's content
   - Sections with only whitespace content (no actual text below the heading) are excluded (per D-12 / PARSE-03)
   - Returns dict keyed by section id: `{"overview": {"label": "Project Overview", "content": "..."}, "mquery": {"label": "M Query Business Logic", "content": "..."}, ...}`
   - If multiple headings match the same section id, their content is concatenated

Module-level imports: `import os, re, yaml`

Then create `scripts/test_md_parser.py` with tests for all 7 behaviors listed above. Use standard unittest. Test data is inline strings written to temp files via `tempfile.NamedTemporaryFile`.
  </action>
  <verify>
    <automated>cd ~/.claude/skills/pbi-docgen && python -m pytest scripts/test_md_parser.py -v</automated>
  </verify>
  <acceptance_criteria>
    - md_parser.py contains "def parse_markdown_sources("
    - md_parser.py contains "def _is_in_code_block("
    - md_parser.py contains "def _match_heading_to_section("
    - md_parser.py imports yaml
    - md_parser.py uses regex pattern r"^##\s+" or equivalent
    - test_md_parser.py contains at least 7 test methods
    - All 7 tests pass when run with pytest
    - parse_markdown_sources returns a dict keyed by section id strings
    - Sections inside fenced code blocks are not detected
    - Empty sections (heading only, no content) are excluded from output
  </acceptance_criteria>
  <done>md_parser.py exports parse_markdown_sources that correctly detects 6 section types from ## headings, handles code block false positives, skips empty sections, and concatenates multiple files; all 7 tests pass</done>
</task>

</tasks>

<verification>
1. `ls ~/.claude/skills/pbi-docgen/scripts/ ~/.claude/skills/pbi-docgen/templates/prompts/ ~/.claude/skills/pbi-docgen/references/` shows all directories exist
2. `python -c "from scripts.utils import parse_hex_color; print(parse_hex_color('#1B365D'))"` from the skill dir outputs `(27, 54, 93)`
3. `cd ~/.claude/skills/pbi-docgen && python -m pytest scripts/test_md_parser.py -v` shows all tests passing
4. `grep -c "def " ~/.claude/skills/pbi-docgen/scripts/utils.py` returns 6
5. `grep -c "def test_" ~/.claude/skills/pbi-docgen/scripts/test_md_parser.py` returns at least 7
</verification>

<success_criteria>
- Skill directory structure exists at ~/.claude/skills/pbi-docgen/ with scripts/, templates/prompts/, references/
- section_heading_map.yaml defines all 6 section categories with keyword arrays
- fowler_guidance.md contains 7 Fowler rules
- utils.py exports 6 helper functions with type hints
- md_parser.py detects sections from ## headings using keyword matching
- Code block false positives are handled (fenced block tracking)
- Empty sections are excluded from output
- Multiple source files are concatenated before parsing
- All parser tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-skill-core-intake-docx/01-01-SUMMARY.md`
</output>
